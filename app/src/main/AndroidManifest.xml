<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="byteinspace.net.eurexcommunicatordb">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IndexTickerActivity"
            android:label="@string/title_activity_index_ticker"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".NewsActivity"
            android:label="@string/news_headline"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".HelloPrivateUserAcivity" />
        <activity
            android:name=".CircularsActivity"
            android:label="@string/title_activity_circulars"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".MailingActivity"
            android:label="@string/mailing_headline"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".EventActivity"
            android:label="@string/events"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".FuturesOverviewActivity" />
        <activity android:name=".EventRegisterActivity" />
        <activity android:name=".SurveysActivity"
        android:label="@string/surveys"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        ></activity>

        <activity android:name=".SurveyDetailActivity"
            android:label="@string/surveys"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            ></activity>
    </application>

</manifest>